import * as vscode from 'vscode';

/**
 * Registers custom context menu actions for contract items in the sidebar.
 * Can be extended to add template-specific or contract-specific actions.
 */
export function registerCustomContextAction(_context: vscode.ExtensionContext): void {
    // Context menu items can be registered here when needed
}
